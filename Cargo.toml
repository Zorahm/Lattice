[workspace]
resolver = "2"
members = [
    "crates/lattice-proto",
    "crates/lattice-server",
    "crates/lattice-client",
]

[workspace.package]
edition = "2021"
rust-version = "1.74"
license = "MIT OR Apache-2.0"

[profile.release]
lto = "thin"
codegen-units = 1
